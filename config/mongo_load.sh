# GA 

# mongoimport --db=mongo-crud --collection=people --type=csv --headerline --file=data/people.csv --drop

# Stack Overflow

# mongoimport --db databaseName --collection collectionName --drop --file ~/path/to/file/fileName.json

# if path isn't working, do path from the root
# run this command in terminal, not the mongo shell
# csv seems to work better